/**
 * Phase 0 stub — throws until implemented in later phases.
 * @see project_docs/implementation-plan.md
 */
export function notImplementedYet(module: string): never {
  throw new Error(
    `[${module}] Not implemented yet. Follow project_docs/implementation-plan.md (Phase 2+).`,
  );
}
